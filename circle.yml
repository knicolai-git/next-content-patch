machine:
  node:
    version: 7.6.0

deployment:
  ci:
    branch: ^(?!master)$
    commands:
      - tasks/deploy --bucketDirectory=circle
  production:
    branch: master
    commands:
      - tasks/deploy --bucketDirectory=prod
